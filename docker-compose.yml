services:
  app:
    image: node:20-slim
    working_dir: /app
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    command: >
      sh -c "npx --yes wrangler pages dev public --ip 0.0.0.0 --port 3000"
